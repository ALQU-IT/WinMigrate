__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
# WinMigrate's own output must never be committed
*.dat
*.manifest.json
winmigrate-*.log
plan.json

# PyInstaller
build/
dist/
*.spec

# Skills installed into this checkout by tooling (npx skills add). They are
# development aids for whoever is working here, not part of the product.
.agents/
.claude/skills/
skills-lock.json

# Build assets, drawn by build_assets.py rather than committed
winmigrate.ico
version_info.txt
